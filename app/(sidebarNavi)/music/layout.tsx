import React from 'react';
import style from "@/app/(sidebarNavi)/recommend/page.module.css";

function Layout({children}: { children: React.ReactNode }) {
    return (
        <>
        </>
    );
}

export default Layout;